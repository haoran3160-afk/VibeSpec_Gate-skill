# trigger-negative-readme

Agent id: `019f7ddb-1c76-79c2-995a-507cd0b2abe7`

Harness: the Skill was installed at the standard user location and available for normal host routing. It was not explicitly invoked or attached to the request. Activation is reported by the executing Agent in its final line.

## Raw Request

Draft a clearer README installation section for a generic Python package installed with `pip install example-package`. Answer in chat only.

## Unedited Final Output

## Installation

Install the package from PyPI:

```bash
pip install example-package
```

To confirm the installation succeeded:

```bash
pip show example-package
```

Using a virtual environment is recommended to keep project dependencies isolated.

VibeSpec Gate activated: no
